import { createTheme } from '@mui/material/styles';
import back_home from '../img/background_home.jpg';

const theme = createTheme({
    palette:{
        primary:{
            main:'#ffffff'
        },
        secondary:{
            main:'#672c70'
        }
    },
})

export default theme;